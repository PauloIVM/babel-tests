// Just run the comand below to transbile without any file config:
// ./node_modules/.bin/babel src --out-dir lib --presets=@babel/preset-react

// Or, run "npm run build" to transpile with a differ framework function

const App = (props) => {
    <div>{"Hello!"}</div>
};